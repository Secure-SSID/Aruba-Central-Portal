# ============================================================================
# Aruba Central API Configuration Example
# ============================================================================
# Copy this file to .env and fill in your credentials
# DO NOT commit the .env file to version control
#
# Get your API credentials from:
# https://central.arubanetworks.com → Account Home → API Gateway → System Apps
# ============================================================================

# ============================================================================
# Docker Configuration
# ============================================================================
# User/Group IDs for file permissions in Docker containers
# Find your UID/GID with: id
# Common values: 1000:1000 (default user), 0:0 (root)
# Leave empty or set to 0:0 to run as root
PUID=1000
PGID=1000

# ============================================================================
# Aruba Central API Configuration
# ============================================================================

# API Base URL (choose based on your region)
# See all available regions in Setup Wizard or documentation
# Common regions:
#   - US: https://apigw-prod2.central.arubanetworks.com
#   - EU: https://apigw-eucentral3.central.arubanetworks.com
#   - APAC: https://apigw-apaceast1.central.arubanetworks.com
#   - Internal: https://internal.api.central.arubanetworks.com
ARUBA_BASE_URL=https://internal.api.central.arubanetworks.com

# OAuth2 Credentials (Required)
# Get these from Aruba Central → API Gateway → System Apps → New App
ARUBA_CLIENT_ID=your_client_id_here
ARUBA_CLIENT_SECRET=your_client_secret_here
ARUBA_CUSTOMER_ID=your_customer_id_here

# ============================================================================
# Optional: User Credentials (for password grant type)
# ============================================================================
# Only required if using password-based authentication
# Leave commented out if using client credentials flow
# ARUBA_USERNAME=your_username
# ARUBA_PASSWORD=your_password

# ============================================================================
# Application Configuration
# ============================================================================

# Logging Configuration
LOG_LEVEL=INFO

# Flask Environment (for dashboard backend)
# Options: development, production
FLASK_ENV=production

# Token Cache Directory (for Docker)
# Default: /app/data in container
# TOKEN_CACHE_DIR=/app/data

# ============================================================================
# Advanced Configuration (Optional)
# ============================================================================

# Request Timeout (seconds)
# Default: 30
# API_TIMEOUT=30

# Max Retry Attempts
# Default: 3
# API_MAX_RETRIES=3

# Rate Limit Handling
# Enable automatic retry with exponential backoff on 429 errors
# Default: true
# API_RATE_LIMIT_RETRY=true

# ============================================================================
# Development Settings
# ============================================================================

# Enable Debug Mode (development only)
# WARNING: Never enable in production - exposes sensitive information
# DEBUG=false

# Python Path (for development)
# PYTHONPATH=/path/to/project

# ============================================================================
# Notes
# ============================================================================
#
# 1. Token Caching:
#    - The API client automatically caches OAuth tokens to prevent rate limiting
#    - Tokens are cached in .token_cache_legacy.json (never commit this file)
#    - Aruba Central has strict rate limits: 1 access token per 30 minutes
#
# 2. Security:
#    - Never commit .env file to version control
#    - Rotate API credentials regularly
#    - Use least-privilege OAuth2 scopes
#    - Store credentials securely in production
#
# 3. Setup Wizard:
#    - Instead of manually editing this file, use the web-based Setup Wizard
#    - Access at http://localhost:5001 after starting Docker containers
#    - The wizard handles all configuration automatically
#
# 4. Docker Volumes:
#    - Token cache is persisted in a Docker volume to prevent rate limiting
#    - Output files are stored in ./output directory
#    - .env file is mounted as read-write for Setup Wizard updates
#
# ============================================================================
